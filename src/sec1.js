import React from 'react'
import * as bs from 'react-bootstrap'
import { hydrate } from 'react-dom'

export default function SECTION(props) {
    return (
        <bs.Button variant = "outline-success" size="lg">
            hey
        </bs.Button>
        //look up about product cards
    )
}